# Copyright 2025 the Kubeapps contributors.
# SPDX-License-Identifier: Apache-2.0

---
name: B&R dep Bitnami Apache Exporter

on:
  workflow_dispatch:
  push:
    tags-ignore:
      - '*'
    paths:
      - 'external/bitnami-deprecated-apache-exporter/1/debian-12/**'
      - '.github/workflows/release-bitnami-apache.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/bitnami-deprecated-apache-exporter
          tags: |
            type=raw,value=1
            type=raw,value=latest

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: external/bitnami-deprecated-apache-exporter/1/debian-12
          file: external/bitnami-deprecated-apache-exporter/1/debian-12/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
