name: B&R dep Bitnami OAuth2 Proxy

on:
  workflow_dispatch:
  push:
    paths:
      - 'external/bitnami-deprecated-oauth2-proxy/oauth2-proxy/7.8/debian-12/**'
      - '.github/workflows/release-bitnami-oauth2-proxy.yml'

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v3
        with:
          images: ghcr.io/${{ github.repository }}/bitnami-deprecated-oauth2-proxy
          tags: |
            type=raw,value=7.12.0
            type=raw,value=latest

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: external/bitnami-deprecated-oauth2-proxy/7/debian-12
          file: external/bitnami-deprecated-oauth2-proxy/7/debian-12/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
