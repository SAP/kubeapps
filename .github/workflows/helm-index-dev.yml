---
name: Helm Dev Index Publisher

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: "Only package this specific pre-release chart version (optional)"
        required: false
        type: string
      full_regeneration:
        description: "Rebuild dev index.yaml from scratch and repackage all pre-release versions"
        required: false
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  publish_dev_index:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
      INPUT_VERSION: ${{ inputs.version }}
      FULL_REGEN: ${{ inputs.full_regeneration }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Install Helm
        run: |
          set -eu
          source ./script/lib/libcitools.sh
          installHelm ${HELM_VERSION_STABLE:-v4.0.0} helm-stable

      - name: Publish dev helm index via script
        run: |
          set -eu
          ./script/helm_index_publish.sh --mode dev

      - name: Show Helm repo add instructions (dev)
        run: |
          echo "Users can run:"
          echo "  helm repo add kubeapps-dev https://${GITHUB_REPOSITORY_OWNER}.github.io/${GITHUB_REPOSITORY#*/}/helm/dev"
          echo "  helm repo update"
